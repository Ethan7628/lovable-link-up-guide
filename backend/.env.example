# MongoDB Atlas Configuration
# Replace with your actual MongoDB Atlas connection string
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/bodyconnect?retryWrites=true&w=majority

# Environment
NODE_ENV=development

# Frontend URL (adjust based on your frontend deployment)
FRONTEND_URL=http://localhost:5173

# CORS Origins (comma-separated list of allowed origins)
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173,https://your-frontend-domain.com

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secure-jwt-secret-key-here

# Stripe Configuration (if using payments)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Server Port
PORT=5000

# MongoDB Atlas Setup Instructions:
# 1. Go to https://www.mongodb.com/cloud/atlas
# 2. Create a new cluster or use an existing one
# 3. Go to "Database Access" and create a database user
# 4. Go to "Network Access" and add 0.0.0.0/0 to allow connections from anywhere
# 5. Get your connection string from "Connect" -> "Connect your application"
# 6. Replace the connection string above with your actual values

# Important Security Notes:
# - Never commit this file with real credentials to version control
# - Use strong passwords for database users
# - Rotate JWT secrets regularly
# - Use environment-specific configuration for production